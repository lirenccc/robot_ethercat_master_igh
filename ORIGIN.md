# Provenance

This repository was **extracted** (file copy) from:

- Source tree: `robotic_joint_control` / package `ethercat_joint_common`
- Backend: **IgH EtherCAT Master** (`master/igh`, `ethercat_servo_igh`)
- Shared support: `motor/`, `util/`, `diagnostics/runtime_logger`, `servo/cia402`

**The source repository was not modified** during extraction.

Vendor / system assets **not** vendored here:

- IgH userspace (`/opt/etherlab` or equivalent: `ecrt.h`, `libethercat`)
- Kernel master module (`ec_master.ko`) and matching device drivers

License of extracted sources: GPL-2.0 (see `LICENSE`). IgH itself is GPL-2.0.
